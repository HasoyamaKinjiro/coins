import React from 'react';
import './Header.css';
import Search from '../Search';

const Header = () => {
    return (
        <div className="header">
            <Search />
        </div>
    );
};

export default Header;
